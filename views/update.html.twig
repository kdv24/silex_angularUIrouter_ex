<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
        <link rel="stylesheet" href="/../css/styles.css">
        <title>Tester whatsit!1!</title>
    </head>
    <body>
        <div class="container">
            <h1>I'm still that h1 in the HTML file.</h1>
            <h2>{{ whoa }}</h2>

            <div class="row">
                <div class="col-md-6">
                    <div id="angular-box" ng-app="thinger">
                        <div ui-view></div>
                    </div>
                </div>

                <div class="col-md-6">
                    <p>But what if we want to add ANOTHER nonsense to the outer page?</p>
                    <form action="/add-thing" method="post">
                        <div class="form-group">
                            <input type="text" name="word" id="word" class="form-control" placeholder="Type literally anything here">
                        </div>
                        <button type="submit" class="btn">gimme</button>
                    </form>
                </div>
            </div>
            <div class="row">
                <ul id="words">
                    <h5>Here are those words you wanted rendered by Twig!</h5>
                    {% for word in words %}
                        <li>{{ word.getName }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <script src="lib/angular.js"></script>
        <script src="lib/angular-ui-router.js"></script>
        <script src="app.js"></script>
        <script src="controllers/OhgodhelpController.js"></script>
        <script src="services/OhgodhelpFactory.js"></script>
    </body>
</html>
